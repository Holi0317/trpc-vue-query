import type { ResolverDef } from "../shared";

/**
 * Decorator for subscriptions.
 *
 * Subscription is not implemented yet (not sure how to implement). Please open
 * an issue if there is demand.
 */
export type DecorateSubscription<_TDef extends ResolverDef> = never;
